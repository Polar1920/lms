<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Cuestionario</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            position: relative;
            background: url('/img/loginBG.png') no-repeat center center fixed;
            background-size: cover;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            /* Capa oscura semi-transparente */
            z-index: 1;
        }

        .container {
            position: relative;
            z-index: 2;
            background: rgba(255, 255, 255, 0.9);
            padding: 20px;
            border-radius: 8px;
        }

        .btn-secondary {
            margin-bottom: 20px;
        }

        img {
            max-width: 50%;
            height: auto;
            border-radius: 8px;
            margin-top: 10px;
            margin-bottom: 10px;
        }

        h1,
        label {
            color: #333;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        input[type="text"],
        textarea,
        input[type="file"] {
            margin-bottom: 15px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            width: 100%;
        }

        button {
            align-self: flex-start;
        }
    </style>
</head>

<body>
    <div class="container">
        <button class="btn btn-secondary" onclick="location.href='/teacher/quizzes/<%= cuestionario.id %>/details/<%= cuestionario.modulo_id %>'">Volver</button>
        <h1>Editar Cuestionario</h1>
        <form action="/teacher/quizzes/<%= cuestionario.id %>/edit" method="post" enctype="multipart/form-data">
            <input type="hidden" name="_method" value="PUT">
            <input type="hidden" name="moduleId" value="<%= cuestionario.modulo_id %>">
            <div class="mb-3">
                <label for="titulo" class="form-label">Título:</label>
                <input type="text" name="titulo" class="form-control" value="<%= cuestionario.titulo %>" required>
            </div>
            <div class="mb-3">
                <label for="descripcion" class="form-label">Descripción:</label>
                <textarea name="descripcion" class="form-control" rows="5" required><%= cuestionario.descripcion %></textarea>
            </div>
            <div class="mb-3">
                <label for="imagenActual" class="form-label">Imagen Actual:</label>
                <img src="<%= cuestionario.imagen %>" alt="Imagen Actual del Cuestionario">
            </div>
            <div class="mb-3">
                <label for="nuevaImagen" class="form-label">Seleccionar Nueva Imagen (opcional):</label>
                <input type="file" name="quizImage" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">Guardar Cambios</button>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
