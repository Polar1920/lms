<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Módulo</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .card-img-top {
            max-height: 200px;
            /* Ajusta el tamaño máximo de la imagen */
            object-fit: cover;
            /* Escala la imagen para cubrir el área especificada */
        }
    </style>
</head>

<body>
    <div class="container">
        <button class="btn btn-secondary" onclick="location.href='/student/dashboard'">Volver al Dashboard</button>
        <h1 class="mt-5">Detalles del Módulo</h1>
        <div class="card mt-3">
            <img src="<%= modulo.imagen %>" class="card-img-top" alt="Imagen del módulo">
            <div class="card-body">
                <h5 class="card-title">
                    <%= modulo.nombre %>
                </h5>
                <p class="card-text">
                    <%= modulo.descripcion %>
                </p>
            </div>
        </div>
        <div class="mt-4">
            <h2>Cuestionarios</h2>
            <% if (modulo.cuestionarios && modulo.cuestionarios.length) { %>
                <% modulo.cuestionarios.forEach(cuestionario=> { %>
                    <div class="card mt-3">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= cuestionario.titulo %>
                            </h5>
                            <% if (cuestionario.imagen) { %>
                                <img src="<%= cuestionario.imagen %>" class="card-img-top" alt="Imagen del Cuestionario">
                            <% } %>
                            <p class="card-text">
                                <%= cuestionario.descripcion %>
                            </p>
                            <p class="card-text">
                                Porcentaje: <%= cuestionario.porcentaje %>
                            </p>
                            <a href="/student/quiz/<%= cuestionario.id %>" class="btn btn-primary">Realizar
                                Cuestionario</a>
                            <a href="/student/quiz/<%= cuestionario.id %>/attempts/<%= cuestionario.modulo_id %>" class="btn btn-primary">Ver Intentos</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <p>No hay cuestionarios disponibles para este módulo.</p>
                            <% } %>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>