<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Intentos del Cuestionario</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container mt-5">
        <button class="btn btn-secondary" onclick="location.href='/student/modules/<%= moduloId %>/details'">Volver</button>
        <h1 class="mt-3">Intentos del Cuestionario</h1>

        <% if (intentos.length> 0) { %>
            <div class="list-group mt-3">
                <% intentos.forEach(intento=> { %>
                    <div class="list-group-item">
                        <h5 class="mb-1">Intento del <%= new Date(intento.createdAt).toLocaleString() %>
                        </h5>
                        <p class="mb-1">Nota: <%= intento.nota %>
                        </p>
                        <% if (intento.comentario_profesor) { %>
                            <p class="mb-1">Comentario del profesor: <%= intento.comentario_profesor %>
                            </p>
                            <% } %>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <p>No hay intentos disponibles para este cuestionario.</p>
                <% } %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>